# SysWisdom Guardrails — Development Instructions

## Project Context
- **Project**: SysWisdom.AI Guardrails — CO-INTELLIGENCE Data Quality Platform
- **Backend**: Express.js 4.18.3 (ES modules, Node 22.13.1)
- **Frontend**: Vanilla JavaScript SPA (public/index.html)
- **Testing**: 11/11 unit tests passing (tests/guardrails.test.js)
- **Company**: SysWisdom.AI LLC, Georgia
- **API Backend**: Private web service (user-owned, Google Cloud)
- **Deployment Model**: Open-source code + closed-source API backend (hybrid)

---

## 🔐 Business Model: Open-Source + API Key

### IP Protection Strategy
Your intellectual property is protected through the **API key gateway**, not through code secrecy:

**Public (Open-Source)**:
- ✅ Express backend code (server.js)
- ✅ Frontend SPA (index.html)
- ✅ Test suites and sample data
- ✅ Documentation and contribution guidelines
- **Why**: Build trust, enable community contributions, attract users

**Private (Closed-Source)**:
- 🔒 Data quality algorithms (Google Cloud)
- 🔒 Machine learning models
- 🔒 Scoring logic and heuristics
- 🔒 User data and analysis records

**Gate**: API Key ($99/month or usage-based)
- Only your backend services require API keys
- Users can fork and run the code but **cannot access the backend without**:
  1. Valid API key (you issue)
  2. Monthly payment (recurring revenue)
  3. Terms of Service acceptance (legal protection)

### Why This Works Better Than SaaS

| Aspect | This Model | Traditional SaaS |
|--------|-----------|-----------------|
| Transparency | Code visible → audit | Black box → risk |
| Community | Fork/contribute → growth | Locked → no contributions |
| Setup | Fork + key (2 min) | Signup + setup (15 min) |
| Security | Self-hosted option | Vendor lock-in |
| Trust | "I can see it running" | "Trust us" ❌ |
| Adoption | Low friction → fast growth | High friction → slow |
| IP Safe | ✅ API key required | ✅ Proprietary code |
| Revenue | Recurring per user | Fixed overhead |

**Real-world examples**: Stripe, Twilio, OpenAI, Auth0, Firebase, Supabase — all use this model at $100B+ scale.

### API Key Management
- **Issuance**: User requests at `sales@syswisdom.ai`
- **Format**: `sk_live_abc123xyz...` (50-char alphanumeric)
- **Validation**: Every `/analyze` call checks API key against your backend
- **Rate Limiting**: Enforce per-key limits (e.g., 100 analyses/hour)
- **Revocation**: Can disable key instantly if terms violated
- **Billing**: Track usage per API key for metering

### Cost Model (Options)
**Option A: Flat Monthly**
- Free tier: 100 analyses/month ($0)
- Pro: $99/month (unlimited)
- Enterprise: $500/month (priority support)

**Option B: Usage-Based** (Better for scaling)
- Free: 100/month ($0)
- Standard: $0.50/analysis (e.g., 200 analyses = $100)
- Volume: $0.30/analysis at 1000+/month
- Enterprise: negotiated

**Current**: Flat $99/month is safe starting point. Switch to usage-based once you have 50+ customers.

---

## 🚀 Optional Add-Ons (Future Revenue)

Once MVP is stable, implement these to 10x revenue:

### 1. API Console Dashboard
Track usage, issue API keys, view billing in real-time.
- **Cost to build**: 1-2 weeks
- **Revenue impact**: $0 (table stakes for Pro tier)
- **Enterprise value**: $$$$ (admins demand dashboards)

### 2. Usage Metering & Billing
Switch from flat $99/month to pay-per-analysis (e.g., $0.50/analysis).
- **Cost to build**: 1 week
- **Revenue impact**: 2-3x higher (users optimize spending)
- **Enterprise demand**: Mandatory for large orgs

### 3. Webhooks & Integrations
Real-time notifications (Slack, email, Jira) when analysis completes.
- **Cost to build**: 2 weeks
- **Revenue impact**: +$100-500/month per customer (premium feature)
- **Enterprise demand**: Required for CI/CD integration

### 4. Scheduled & Recurring Analysis
Auto-run daily/weekly/monthly quality checks on key datasets.
- **Cost to build**: 1 week
- **Revenue impact**: Premium tier only (+$50/month)
- **Developer demand**: Huge (ops automation)

**Roadmap**: Build in order (Console → Metering → Webhooks → Scheduled) over next 3 months.

---

## Current Architecture

### Backend (server.js)
- **Module System**: ES modules (import/export)
- **Entry Point**: Dynamic detection (`import.meta.url` with fallback to `process.argv[1]`)
- **Endpoints** (7 total):
  1. `POST /analyze` — File upload to SysWisdom API, normalize issues
  2. `POST /approval` — Log human decision (approved/rejected)
  3. `GET /fix-library` — List all fixes sorted by votes (descending)
  4. `POST /fix-library` — Add new fix to library
  5. `POST /fix-library/vote` — Increment/decrement fix votes
  6. `GET /report/:id` — Generate plain-text quality report
  7. `GET /history` — Summarize all analyses (approved/rejected/pending counts)
  8. `GET /drift-analysis` — Monthly accuracy tracking + drift detection (>10% drop)
  9. `GET /training-export` — Export labeled dataset for ML retraining

### Data Files
- **`data/analyses.json`** — Records all uploaded files + API results
- **`data/fix-library.json`** — 7 seeded fixes with vote counts
- **Schema**: Each fix has `{ fix_id, issue_type, column, fix_description, votes, confidence, created_by, created_at }`

### Frontend (public/index.html)
- **Tabs**: Upload, Review, Fix Library, Report, History, Drift Monitor
- **Branding**: SysWisdom.AI throughout (title, header, footer with copyright)
- **Footer**: LLC, Georgia location, contact emails, links to Privacy/TOS

## Current Test Suite (11/11 Passing)
- TEST 1: `normalizeIssues` maps issue types
- TEST 2: Severity thresholds (CRITICAL/HIGH/MEDIUM)
- TEST 3: POST /analyze rejects no-file (400)
- TEST 4: POST /approval validates schema (400)
- TEST 5: GET /fix-library returns sorted fixes
- TEST 6: GET /report/:id generates reports with branding
- TEST 7: GET /history maintains invariant (approved+rejected+pending=total)
- TEST 8: POST /fix-library/vote increments/decrements
- TEST 9: POST /fix-library validates schema
- TEST 10: GET /drift-analysis returns monthly accuracy + drift status
- TEST 11: GET /training-export returns labeled dataset

## Next Priority: Priority 1b — Custom Rules Endpoint

### Requirements
**New Endpoint: `POST /custom-rules`**
- **Purpose**: Allow users to define custom validation rules (name, pattern, column, severity)
- **Schema**:
  ```javascript
  {
    name: string,           // "Mixed Types in Amount", "Missing Dates", etc.
    column_pattern: string, // "Amount", "Date", or regex pattern
    rule_type: enum,        // "pattern", "datatype", "range", "custom"
    pattern: string,        // regex or description of rule
    severity: enum,         // "CRITICAL", "HIGH", "MEDIUM"
    auto_apply: boolean     // true if fix should apply automatically
  }
  ```

- **Return**: `{ rule_id, created_at, name, severity, auto_apply }`
- **Persist**: Save to `data/custom-rules.json` (lightweight database)
- **Behavior**: Apply rules to future analyses automatically

**New Endpoint: `GET /custom-rules`**
- **Purpose**: List all currently active custom rules
- **Return**: Array of rule objects with vote counts if applicable

### Implementation Tasks
1. Create `data/custom-rules.json` with seed rules (or empty array)
2. Add helper functions: `readCustomRules()`, `writeCustomRules()`, `generateRuleId()`
3. Implement `POST /custom-rules` endpoint with validation
4. Implement `GET /custom-rules` endpoint
5. Integrate custom rules into the `/analyze` pipeline (check custom rules against uploaded data)
6. Add Unit Test 12: `POST /custom-rules creates rule with rule_id`
7. Add Unit Test 13: `GET /custom-rules returns all rules in order`
8. Frontend integration: Add "Custom Rules" tab or modal in index.html

### Code Style
- **Language**: JavaScript (ES modules)
- **Naming**: camelCase for functions, UPPER_CASE for constants
- **Error Handling**: Return HTTP status codes (400 for validation, 404 for not found, 500 for server error)
- **File Paths**: Use getters for dynamic paths (e.g., `getCustomRulesFile()`)
- **Comments**: Brief JSDoc + inline comments for complex logic

## For Copilot Chat: Feature Implementation Prompt

When implementing Priority 1b, use this prompt structure:

```
I need to implement the /custom-rules endpoint for the SysWisdom Guardrails platform.

CONTEXT:
- Backend: Express.js ES modules (Node 22.13.1)
- Database: `data/fix-library.json` and `data/analyses.json` for reference
- Current endpoints: /analyze, /approval, /fix-library, GET/POST, /report, /history, /drift-analysis, /training-export
- Test Suite: 11/11 tests passing (tests/guardrails.test.js)

REQUIREMENTS:
1. Create POST /custom-rules endpoint
   - Accept: { name, column_pattern, rule_type, pattern, severity, auto_apply }
   - Return: { rule_id, created_at, name, severity, auto_apply }
   - Persist to data/custom-rules.json

2. Create GET /custom-rules endpoint
   - Return: Array of all custom rules

3. Add Unit Tests (TEST 12 + TEST 13)
   - TEST 12: POST creates rule with rule_id
   - TEST 13: GET returns all rules

CONSTRAINTS:
- Must be ES modules (import/export)
- Follow existing code style in server.js
- Handle file I/O errors gracefully
- Validate input schema before persisting

Please implement these endpoints and tests.
```

## Key Files to Reference
- **server.js**: Main backend (1,400+ lines, all 9 endpoints)
- **public/index.html**: Frontend SPA (1,275 lines)
- **tests/guardrails.test.js**: Unit test suite (11/11 passing)
- **data/fix-library.json**: Fix library database (7 seeded fixes)
- **data/analyses.json**: Analyses database (sample records)
- **package.json**: Dependencies (Express 4.18.3, formidable 2.1.1)
- **PRIVACY.md**: Privacy policy (company branding)
- **TERMS_OF_SERVICE.md**: Terms of service (company branding)

## Wisdom Formula Context
The platform embodies: **Systemic Wisdom = (Experience / Wisdom) ^ Time**

- **Experience**: Each fix applied, each rule created, each analysis performed
- **Wisdom**: User expertise captured in custom rules + approved fixes
- **Time**: Iteration → learning → better automatic suggestions
- **Goal**: System learns faster as users apply fixes and define custom rules

---

## Next Feature After Custom Rules: Priority 1c — Automated Feedback Loop

Once custom rules are complete, the next feature will automate the feedback loop:
- Apply fixes to original data → rerun analysis → measure improvement
- Lightweight database tracking which fixes work best
- ML integration: export fix impact history for model retraining
- UI: Show which fixes have highest positive impact

This will enable the system to learn which custom rules and fixes are most effective over time.
